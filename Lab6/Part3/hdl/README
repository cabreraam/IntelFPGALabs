Easy to think about how the chain of adds work if you just do a[3:0] x b[0].
Think about how that works, and then extend to b[1:0];

When I describe rows RE: full adders, I mean rows relative to Figure 3c, and
columns are relative to bit position
